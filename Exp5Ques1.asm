DATA SEGMENT
    NUM1 DW 25
    NUM2 DW 50    
    PROD DW ?
    GCD DW ?
    LCM DW ?
DATA ENDS

CODE SEGMENT
    ASSUME CS:CODE,DS:DATA
    START:
    MOV AX,DATA
    MOV DS,AX
    MOV AX,NUM1  
    MOV CX,NUM2
    MUL CX 
    MOV PROD,AX 
    MOV AX,NUM1 
    MOV BX,NUM2
    
ABC:CMP AX,BX
    JZ RESULT
    JNC DIVISION
    XCHG AX,BX 
    
DIVISION:MOV DX,0000H
     DIV BX
     CMP DX,0000H
     JZ RESULT
     MOV AX,DX
     JMP ABC
     
RESULT:MOV GCD,BX 
     MOV AX,PROD
     DIV BX
     MOV LCM,AX

     MOV AH,4CH
     INT 21H
CODE ENDS
END START
 